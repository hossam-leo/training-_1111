CREATE TABLE IF NOT EXISTS reviews (session_id TEXT PRIMARY KEY REFERENCES sessions(id) ON DELETE CASCADE, reviewer_id TEXT NOT NULL, decision TEXT NOT NULL, notes TEXT NOT NULL, created_at TIMESTAMPTZ NOT NULL);
